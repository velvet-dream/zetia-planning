{% extends 'base.html.twig' %}

{% block title %}
	Éditer un projet
{% endblock %}

{% block main %}
	{# potentiellement à mettre plutôt dans un drawer #}
	<twig:HomeContainer title="Éditer un projet">
		<twig:block name="extra">
			{{ include('project/delete.html.twig') }}
			<a href="{{ path('pct_index') }}" aria-label="Retour à la liste des projets">Retour</a>
		</twig:block>

		{{ include('app/form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
	</twig:HomeContainer>
{% endblock %}
