{% extends 'base.html.twig' %}

{% block main %}
<div class="dashboard">
 

    <div class="main-content">
        <header>
            <div class="search-bar">
                <input type="text" placeholder="Rechercher">
                <button><i class="fas fa-search"></i></button>
            </div>
            <div class="settings"><i class="fas fa-cog"></i></div>
        </header>

          <section class="projects">
        {% for project in projects %}
            <div class="project">
                <div class="icon"><i class="fas fa-folder"></i></div>
                <h3>{{ project.pctTitle }}</h3>
            </div>
        {% endfor %}
    </section>

        <section class="statistics">
            <!-- Statistiques ici -->
        </section>

        <section class="tasks">
            <h2>Tâches par projets</h2>
            <div class="tabs">
                <button>Nom projet 1</button>
                <button>Nom projet 2</button>
                <button>Nom projet 3</button>
                <button>Nom projet 4</button>
            </div>
            <table class="task-table">
                <thead>
                    <tr>
                        <th>Noms</th>
                        <th>Date de début</th>
                        <th>Date de fin</th>
                        <th>Assignation</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for task in tasks %}
                        <tr>
                            <td>{{ task.tskTitle }}</td>
                            <td>{{ task.tskDateDebut|date('d/m/Y') }}</td>
                            <td>{{ task.tskDateFinPrevisionnelle|date('d/m/Y') }}</td>
                            <td>
                                {% for user in task.users %}
                                    <div class="icon"><i class="fas fa-user"></i> {{ user.usrName }}</div>
                                {% endfor %}
                            </td>
                            <td>
                                <a href="{{ path('ViewTask', {'tskId': task.tskId}) }}"><i class="fas fa-eye"></i></a>
                                <a href="{{ path('editTask', {'tskId': task.tskId}) }}"><i class="fas fa-edit"></i></a>
                                <a href="{{ path('deleteTask', {'tskId': task.tskId}) }}"><i class="fas fa-trash-alt"></i></a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </section>
    </div>
</div>
{% endblock %}
